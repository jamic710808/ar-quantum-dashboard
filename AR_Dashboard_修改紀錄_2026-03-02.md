# AR 儀表板修改紀錄

**修改日期：** 2026-03-02
**修改版本：** V3.0 Ultimate → V3.0 Ultimate（資料匯入版）
**修改人：** Antigravity AI

---

## 修改背景

V3.0 Ultimate 版本雖具備完整的視覺化看板架構（4 個頁籤、10+ 圖表），但所有資料均為硬編碼於 JavaScript 中，使用者無法更新資料。本次修改目標：

1. **製作 Excel 範本資料檔案**，讓使用者可依格式填入實際資料
2. **新增動態資料匯入功能**，匯入後自動刷新所有 KPI、圖表與表格

---

## 新增／修改檔案

| 類型 | 檔名 | 說明 |
|------|------|------|
| **新增** | `AR_Template_V3_0.xlsx` | Excel 範本，含 6 個工作表，附範例資料與說明 |
| **修改** | `AR_Dashboard_V3_0_Ultimate.html` | 新增資料匯入引擎（71 KB → 87 KB，1,604 行 → 2,081 行）|

---

## 修改內容詳述

### 一、Excel 範本檔案（AR_Template_V3_0.xlsx）

新增 6 個色彩編碼工作表：

| 工作表 | 標籤色 | 欄位說明 |
|--------|--------|----------|
| ① 客戶資料 | 金色 | 客戶名稱、城市代碼、應收餘額(M)、加權帳齡(天)、信用資金成本率、風險等級、預計壞帳損失(K)、建議策略 |
| ② 月趨勢 | 綠色 | 月份(YYYY-MM)、應收餘額(M)、DSO(天) |
| ③ 帳齡結構 | 橙色 | 月份、未逾期%、1-30天%、31-90天%、91-365天%、365天以上% |
| ④ 帳齡明細 | 紅色 | 區間名稱、金額(M)、佔比%、壞帳率%、預計損失(K) |
| ⑤ 總覽參數 | 青色 | 累計營收、年化利率、目標DSO、目標AR比率、壞帳預備率、行業平均DSO、資料截止月份 |
| ⑥ 說明 | 灰色 | 填寫規則、城市/風險等級代碼、匯入步驟說明 |

- 每個工作表均含範例資料（對應 V3.0 原始內建資料，共 12 筆客戶）
- 深色暗系配色（與看板視覺風格一致）
- 每張表底部有「填寫說明」備註列

---

### 二、HTML 看板新增功能

#### 📊 資料匯入工具列（Import Toolbar）

**位置：** 看板頁首（Header）正下方、全域篩選器上方

**包含元素：**

| 元素 | 功能 |
|------|------|
| `📥 匯入 Excel` | 開啟系統檔案選擇器，選取 `.xlsx` 檔案後自動解析 |
| `📤 匯出 JSON` | 將當前看板所有資料匯出為 JSON 備份檔 |
| `🔄 還原預設` | 一鍵恢復至原始內建範本資料 |
| 狀態列 | 匯入後顯示成功（綠色）或錯誤（紅色）訊息，6 秒後自動淡出 |
| 說明文字 | 提示支援格式與用途 |

**CSS 特性：** Glassmorphism 風格、Hover 光暈動效、適配現有深色主題

---

#### ⚡ 資料解析引擎（Import Engine）

新增 JavaScript 函式如下：

| 函式 | 說明 |
|------|------|
| `handleFileImport(event)` | 讀取使用者選取的 Excel 檔案（FileReader API） |
| `sheetToObjects(workbook, sheetName)` | 用 SheetJS 將工作表轉為二維陣列 |
| `parseImportedData(workbook)` | 解析 5 個工作表並更新對應資料變數 |
| `refreshDashboard()` | 全域刷新：重算 KPI → 更新 DOM → 重繪圖表 |
| `updateAgingBucketCards()` | 動態重建帳齡明細卡片 HTML |
| `exportCurrentData()` | 匯出當前看板資料為 JSON |
| `resetToDefault()` | 還原為預設內建資料 |
| `showImportStatus(msg, type)` | 顯示匯入結果狀態訊息 |

---

#### 🔄 全域刷新機制（refreshDashboard）

匯入後依下列順序自動更新：

1. 城市篩選器選項（根據客戶城市動態生成）
2. Header 三欄統計（應收總額、逾期客戶數、高風險客戶數）
3. Tab 1 七個 KPI 卡片（應收餘額、累計營收、AR 比率、周轉率、DSO、加權帳齡、信用成本）
4. 帳齡明細五張卡片（動態 HTML）
5. 應收餘額 & DSO 趨勢圖（支援可變月數）
6. 帳齡結構 100% 堆疊圖
7. 各客戶帳齡橫條圖
8. 客戶風險氣泡散點矩陣
9. 客戶風險明細表（可排序）
10. 催收優先序列

---

#### 🔄 函式覆寫機制

`drawTrend()` 與 `drawAgingStack()` 採用覆寫（override）模式：
- 保留原始函式參考（`_origDrawTrend`、`_origDrawAgingStack`）
- 新版本使用動態全域變數（`TREND_AR`、`TREND_DSO`、`AGING_STACK_DATA`、`MONTHS`），支援匯入後資料變更

---

#### 📦 外部依賴

| 函式庫 | 版本 | 載入方式 | 用途 |
|--------|------|----------|------|
| SheetJS (xlsx) | 0.20.3 | CDN（`cdn.sheetjs.com`） | 解析 `.xlsx` 檔案 |

> 注意：需要網際網路連線以載入 SheetJS。離線環境可下載 `xlsx.full.min.js` 並改為本地引用。

---

## 新增全域資料變數

| 變數 | 說明 | 初始值來源 |
|------|------|-----------|
| `TREND_AR` | 近期月份 AR 餘額陣列 | 原始硬編碼資料 |
| `TREND_DSO` | 近期月份 DSO 陣列 | 原始硬編碼資料 |
| `AGING_STACK_DATA` | 帳齡結構百分比（二維陣列）| 原始硬編碼資料 |
| `AGING_BUCKETS` | 帳齡明細物件陣列（5 個區間）| 原始硬編碼資料 |
| `GLOBAL_PARAMS` | 全域 KPI 參數物件 | 原始硬編碼資料 |
| `DEFAULT_CLIENTS` | 預設客戶資料備份（深複製）| 初始化時建立 |
| `DEFAULT_MONTHS` | 預設月份陣列備份 | 初始化時建立 |

---

## 驗收結果（2026-03-02）

**關鍵字驗證（19 項）：**

| 項目 | 結果 |
|------|------|
| SheetJS CDN 引入 | ✅ |
| 匯入工具列 CSS | ✅ |
| 匯入 Excel 按鈕 | ✅ |
| 匯出 JSON 按鈕 | ✅ |
| 還原預設按鈕 | ✅ |
| `handleFileImport` 函式 | ✅ |
| `parseImportedData` 函式 | ✅ |
| `refreshDashboard` 函式 | ✅ |
| `sheetToObjects` 函式 | ✅ |
| 客戶資料工作表解析 | ✅ |
| 月趨勢工作表解析 | ✅ |
| 帳齡結構工作表解析 | ✅ |
| 帳齡明細工作表解析 | ✅ |
| 總覽參數工作表解析 | ✅ |
| `updateAgingBucketCards` 函式 | ✅ |
| `exportCurrentData` 函式 | ✅ |
| `resetToDefault` 函式 | ✅ |
| `drawTrend` 覆寫 | ✅ |
| `import-status` 狀態元素 | ✅ |
| **共 19 項全部通過** | ✅ |

**程式碼規模：**

| | V3.0 原版 | V3.0 資料匯入版 |
|--|------|------|
| 行數 | 1,604 行 | 2,081 行 |
| 大小 | 71 KB | 87 KB |
| 外部依賴 | 0 個 | 1 個（SheetJS CDN） |

---

## 使用者操作指南

**Step 1** — 在資料夾中找到 `AR_Template_V3_0.xlsx`，依照各工作表說明填入實際資料

**Step 2** — 用瀏覽器（Chrome 建議）開啟 `AR_Dashboard_V3_0_Ultimate.html`

**Step 3** — 點擊「📥 匯入 Excel」，選擇填妥的範本檔案

**Step 4** — 等候 1~3 秒後，看板所有圖表、KPI 與表格即時更新 ✨

**Step 5（可選）** — 點擊「📤 匯出 JSON」備份當前資料；「🔄 還原預設」可隨時恢復

---

*本記錄由 Antigravity AI 自動產生，2026-03-02*
